# Epicenter Environment Variables
# Copy this file to .env and fill in your values

# Environment
NODE_ENV=development

# Database (Neon PostgreSQL)
# Sign up for free at https://neon.tech
# Create a new database and copy the connection string
DATABASE_URL=postgresql://user:password@host.neon.tech/database?sslmode=require

# Authentication
# The API server URL (local development)
BETTER_AUTH_URL=http://localhost:8787
# Generate a random secret: openssl rand -base64 32
BETTER_AUTH_SECRET=your-random-secret-here-change-this

# GitHub OAuth
# Create a new OAuth App at https://github.com/settings/applications/new
# Homepage URL: http://localhost:5173
# Authorization callback URL: http://localhost:8787/api/auth/callback/github
GITHUB_CLIENT_ID=your-github-oauth-app-client-id
GITHUB_CLIENT_SECRET=your-github-oauth-app-client-secret